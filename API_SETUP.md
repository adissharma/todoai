# API Setup

To enable the real AI features, you need to add your Gemini API Key.

1. Open `.env.local` in the root of the project.
2. Add your key:
   GEMINI_API_KEY=AIza....
   ```
3. Restart the server if needed (though Next.js usually picks it up).
